#ifndef HEADERUTIL_H
#define HEADERUTIL_H

#include "runtime.h"

class HeaderUtil
{
public:
    static QPixmap userHeader(qint64 id);

    static QPixmap groupHeader(qint64 id);

};

#endif // HEADERUTIL_H
